# Dribbble — аналитика, результат, оценка

Не процесс ради процесса. Система измерения: что сработало → сколько → вердикт.
Меряем там, где есть данные и где деньги, а не там, где красиво.

---

## Честный предел

**Dribbble не отдаёт аналитику по API** — ни лайков, ни просмотров, ни saves. Триггер «New Shot»
даёт только метаданные (заголовок, теги, дата, картинка). Поэтому:
- вовлечение Dribbble (лайки/просмотры) — **вносишь вручную, раз в неделю, 2 мин** (или не меряем вовсе);
- реальную оценку строим на **кликах и заявках**, а не на лайках.

---

## Три слоя метрик

### Слой 1 — Активность (автомат)
Zapier: `New Shot` → строка в Google Sheet / Zapier Table.
Колонки: `date, title, url, tag_set_id, tags, channel`.
Меряет: ритм (шотов в мес.), какие наборы тегов использованы. Ноль твоих усилий.

### Слой 2 — Вовлечение (частично автомат)
- **Pinterest** (подключён): raw API GET → показы, saves, клики по пину. Автомат.
- **Windsor.ai** (подключён): читает Instagram/Pinterest organic + GA4 в одну точку.
- **Dribbble likes/views**: вручную в тот же Sheet, раз в неделю.

### Слой 3 — Результат = деньги (главный)
- **UTM-ссылка** в каждом разлёте: `?utm_source=dribbble&utm_medium=shot&utm_campaign=<tag_set_id>`.
- **GA4 через Windsor.ai**: сколько кликов дошло до сайта/Contra, с какого шота/набора тегов.
- **Ручной лог**: DM / заявки / звонки — 1 строка на событие.

---

## СКОРИНГ ШОТА (то, чем оцениваем)

На каждый шот считаем **RESULT SCORE**, не лайки:

```
RESULT = (клики_на_профиль_или_сайт × 3) + (заявки_DM × 20) + (звонки × 60)
ENGAGE = saves + (лайки ÷ 5)          ← вторично, только как ранний сигнал
```

- Деньгоносные события весят в разы больше вовлечения. Заявка > 100 лайков.
- Скоринг ведём **по tag_set_id**, а не по одному шоту — чтобы сравнивать наборы тегов, а не случай.

### Вердикт по набору тегов (после 3–4 шотов на наборе)
| RESULT на шот | Вердикт |
|---|---|
| 0 кликов, только лайки | **KILL** — набор тянет зрителя, не покупателя. Сменить теги. |
| есть клики, 0 заявок | **ITERATE** — находят, но не пишут. Проблема в подписи/оффере, не в теге. |
| ≥1 заявка на 3 шота | **KEEP + SCALE** — этот набор приносит. Лить в него. |

---

## Еженедельный разбор (5 минут, вердикт а не созерцание)

Раз в неделю смотришь одну строку-сводку:
1. Сколько кликов на профиль/сайт за неделю (GA4/Windsor) и с каких шотов.
2. Сколько заявок/звонков (ручной лог).
3. Обновляешь лайки/views у последних шотов (2 мин).
4. **Одно решение:** какой tag_set_id → KEEP / ITERATE / KILL. Всё.

Цель дня-30: не «лайки выросли», а **N кликов на Contra/сайт и M заявок** — вот числа, по которым
меряем, окупилось ли время.

---

## Что автоматизирую я / что в Zapier

| Кусок | Кто |
|---|---|
| Лог шотов (New Shot → таблица) | Zapier Zap (веб-интерфейс) |
| UTM в подписях разлёта | шаблон подписи + Buffer/каналы |
| Сбор GA4 / organic в одну точку | **Windsor.ai** (подключён) — могу тянуть отчёты отсюда |
| Pinterest pin-статы | Pinterest raw API GET через Zapier |
| Скоринг-таблица + формулы | соберу шаблон (Sheet), пришлю |
| Недельная сводка | считается формулой в той же таблице |

Semrush (объёмы поисковых запросов под теги) — сейчас без API-юнитов: https://www.semrush.com/mcp-access

---

## Приоритет
Аналитика нужна, когда есть поток шотов. Сначала: залить 4 продуктовых шота + собрать лог-Zap.
Скоринг включается со 2–3 недели, когда накопятся клики. Меряем результат (клики/заявки), не лайки.
Деньги по-прежнему: Contra/Braintrust/Upwork — Dribbble витрина, аналитика витрины честная, но вторичная.
